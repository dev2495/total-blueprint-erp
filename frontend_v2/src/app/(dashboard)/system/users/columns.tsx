// Legacy data-table column definitions for the old DataTable-based user list.
// The current list is card-based (see ./page.tsx) — this file is intentionally
// empty to avoid breaking any stale imports without forcing a hard delete.
export {}
